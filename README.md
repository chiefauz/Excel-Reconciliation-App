# Excel Reconciliation App v7

This version adds a reconciliation-header fallback. If the labeled header row is not found reliably, it detects the first run of transaction-like rows and uses the row above as the header.
